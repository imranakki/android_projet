<?xml version="1.0" encoding="utf-8"?>
<!--
    Grade list row — Modern v2
    Glass card with icon, subject, colored score badge
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="14dp"
    android:gravity="center_vertical"
    android:background="@drawable/card_glass"
    android:layout_marginBottom="2dp">

    <!-- Like / dislike icon set dynamically in NoteAdapter -->
    <ImageView
        android:id="@+id/imageViewNoteIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_like"
        android:layout_marginEnd="14dp" />

    <!-- Dot separator -->
    <View
        android:layout_width="3dp"
        android:layout_height="3dp"
        android:background="#44FFFFFF"
        android:layout_marginEnd="14dp" />

    <!-- Subject name -->
    <TextView
        android:id="@+id/textViewMatiere"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Matière"
        android:textColor="#EEEEFF"
        android:textSize="14sp"
        android:textStyle="bold" />

    <!-- Score pill -->
    <TextView
        android:id="@+id/textViewNote"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:gravity="center"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:text="00/20"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold"
        android:background="@drawable/btn_accent" />

</LinearLayout>
